name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install
    
  test:
    runs-on: windows-latest

    steps:
      # - name: Checkout code
      #   uses: actions/checkout@v3

      # - name: Install dependencies
      #   run: npm ci
       
        - name: Set up working directory
          run: cd D:\a\KIGS_POC\KIGS_POC
        - name: Run npm test
          run: npm test
    

      
